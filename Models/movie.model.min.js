import mongoose from "mongoose";

const movieModel = mongoose.Schema({
    title: String,
    percentage: Number,
    year: Number,
    rating:Number,
    image:String,
    userId: String
})

export default mongoose.model('movie', movieModel, 'movies')